# This file contains metadata for your plugin. Beginning
# with version 1.8 this is the preferred way to supply information about a
# plugin. The current method of embedding metadata in __init__.py will
# be supported until version 2.0

# This file should be included when you package your plugin.

# Mandatory items:


[general]
name=Coloring a map
qgisMinimumVersion=2.0
description=Coloring a map such that two contiguous polygons do not have the same color. The algorithm does not always provide a solution with a minimum number of colors.
version=0.2
about=Coloring a map such that two contiguous polygons do not have the same color. The algorithm does not always provide a solution with a minimum number of colors.

# end of mandatory metadata

# Optional items:

# Uncomment the following line and add your changelog entries:
changelog= 0.2 > upgrade to 2.x versions by régis Haubourg 

# tags are comma separated with spaces allowed
tags=

homepage=
tracker=
repository=
icon=icon.png
# experimental flag
experimental=True

# deprecated flag (applies to the whole plugin, not just a single version
deprecated=False

# Author contact information
author=Alain Delplanque
email=alaindelplanque@laposte.net
